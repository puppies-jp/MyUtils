# バイナリファイル解析

Linuxにおけるファイルの解析方法をまとめる

## バイナリがなんのファイルか調べる(**file**コマンド)

以下のコマンドでファイルの形式を読み取り、テキスト,画像,動画といった分類を調べてくれる

```bash
file "ファイル名"
```

## ファイルをバイナリ形式で確認する(**hexdump**コマンド)

ファイルのバイナリを以下のコマンドで調べることができる。
Linuxの実行形式であれば`ELF`の文字列が確認できるはず

```bash
hexdump -C "ファイル名"
```
